version: '3.8'

services:
  gantt-chart:
    build: .
    container_name: gantt-chart
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3001
      - DATA_PATH=/app/data/gantt.db
      - FRONTEND_DIR=/app/frontend
    restart: unless-stopped
