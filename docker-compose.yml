version: '3.8'

services:
  gantt-chart:
    build: .
    container_name: gantt-chart
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
      - DB_PATH=/app/data/gantt.db
      - PORT=3001
    restart: unless-stopped
